#ifndef CONFIG_H
#define CONFIG_H

#include <stdlib.h>

////////////////////////////////////////////////////////////////////
// Luminary
////////////////////////////////////////////////////////////////////

#define LUMINARY_COMPILER  \
  "${CMAKE_C_COMPILER_ID}" \
  " "                      \
  "${CMAKE_C_COMPILER_VERSION}"
#define LUMINARY_OS      \
  "${CMAKE_SYSTEM_NAME}" \
  " "                    \
  "${CMAKE_SYSTEM_VERSION}"
#define LUMINARY_VERSION_DATE "${GIT_COMMIT_DATE}"
#define LUMINARY_BRANCH_NAME "${GIT_BRANCH_NAME}"
#define LUMINARY_CUDA_VERSION "${LUMINARY_CUDA_VERSION}"
#define LUMINARY_OPTIX_VERSION "${LUMINARY_OPTIX_VERSION}"
#define LUMINARY_VERSION_HASH "${GIT_COMMIT_HASH}"
#define LUMINARY_VERSION "${PROJECT_VERSION}"

#endif /* CONFIG_H */
